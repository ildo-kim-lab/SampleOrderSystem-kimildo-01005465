# PLAN.md (이번 증분)

## 목표
`register_sample`에서 평균 생산시간/수율에 숫자가 아닌 값을 입력하면,
크래시 없이 안내 메시지가 출력된다.

## 검증할 동작
`register_sample`에 평균 생산시간으로 숫자가 아닌 문자열("abc")을 입력하면,
`ValueError`가 그대로 튀어나오지 않고 안내 메시지가 `output_func`로
출력된다.

## 근거
- `CLAUDE.md`의 견고성 원칙(콘솔 계층이 잘못된 입력에도 죽지 않아야 한다)을
  숫자 입력 검증까지 확장한다.

## 범위 외
- `create_order`(주문 수량), 다른 숫자 입력 지점의 동일한 검증은 다음
  증분에서 다룬다.

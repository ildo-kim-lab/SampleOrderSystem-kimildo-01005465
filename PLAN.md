# PLAN.md (이번 증분)

## 목표
메인 루프에서 "2"(주문)를 선택하면 실제로 `run_order_menu`가 호출되어
하위 메뉴로 진입한다.

## 검증할 동작
`run_app`에 입력으로 "2"(주문 진입), "1"(접수), 접수에 필요한 값들,
"0"(주문에서 뒤로가기), "0"(프로그램 종료)을 주면, 주문이
`state.order_registry`에 등록된다.

## 근거
- `docs/PRD.md` 5.1 메인 메뉴, 5.3 시료 주문(예약)

## 범위 외
- 모니터링/출고 처리/생산 라인 메뉴의 실제 라우팅 연결은 다음 증분들에서 다룬다.

# PLAN.md (이번 증분)

## 목표
콘솔에서 이미 등록된 시료 ID로 등록을 시도하면, 예외가 그대로 튀어나오지
않고 사용자에게 안내 메시지가 출력된다.

## 검증할 동작
`register_sample`에 이미 등록된 시료와 같은 ID를 입력하면, 크래시 없이
안내 메시지가 `output_func`로 출력된다.

## 근거
- 이전 증분(`SampleRegistry.register()` 중복 ID 검증)에서 "범위 외"로 남겨둔
  콘솔 계층의 사용자 친화적 처리

## 범위 외
- 없음 (이 증분으로 중복 ID 처리 관련 항목이 마무리된다).

# PLAN.md (이번 증분)

## 목표
주문 하위 루프에서 "0"(뒤로가기)은 즉시 반환하고, "1"(접수)을 선택하면
실제로 `create_order`가 호출되어 주문이 등록된다.

## 검증할 동작
`run_order_menu`에 입력으로 "1", 접수에 필요한 값들(시료 ID/고객명/수량),
"0"(뒤로가기)을 주면, `state.order_registry`에 새 주문이 등록된다.

## 근거
- `docs/PRD.md` 5.3 시료 주문 (예약)

## 범위 외
- 승인/거절 라우팅(대상 주문 선택 포함)은 다음 증분들에서 다룬다.

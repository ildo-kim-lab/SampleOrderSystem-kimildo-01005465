# PLAN.md (이번 증분)

## 목표
`_select_order_by_status`에서 대상 상태의 주문이 하나도 없을 때, 목록을
출력한 뒤 곧바로 `None`을 반환한다 (입력을 추가로 요구하지 않고, 잘못된
인덱스로 `IndexError`가 나지 않도록 한다).

## 검증할 동작
승인 대상(RESERVED) 주문이 없는 상태에서 "승인"을 선택하면, 추가 번호 입력
없이 안내 메시지가 출력되고 주문 메뉴로 돌아간다.

## 근거
- `CLAUDE.md`의 견고성 원칙(콘솔 계층이 잘못된 입력에도 죽지 않아야 한다)을
  대상 목록이 비어있는 경우까지 확장한다.

## 범위 외
- 잘못된 인덱스(범위 밖 번호)나 숫자가 아닌 입력에 대한 처리는 다음
  증분에서 다룬다.

# PLAN.md (이번 증분)

## 목표
시료의 재고를 특정 수량만큼 차감할 수 있다 (출고 처리 시 사용될 기반 동작).

## 검증할 동작
`Sample.decrease_stock(quantity)`를 호출하면 `stock`이 그 수량만큼 줄어든다.

## 근거
- `docs/PRD.md` 3. 용어집 — 재고(Stock): 시료별로 현재 출고 가능한 보유 수량
- `docs/PRD.md` 5.7 출고 처리 (출고 시 재고가 실제로 줄어드는 근거 동작)

## 범위 외
- 출고 처리(Order.release())와의 실제 연동, 재고 증가(생산 완료 시),
  재고 부족 시의 예외 처리는 다음 증분에서 다룬다.

# PLAN.md (이번 증분)

## 목표
프로그램 실행 중 필요한 저장소들(시료, 주문, 생산 큐, 생산 현황)을 하나의
상태 객체로 묶어서 관리할 수 있다 (메인 루프 구현의 전제 조건).

## 검증할 동작
`AppState()`를 생성하면 `sample_registry`, `order_registry`,
`production_queue`, `production_line` 각각이 비어있는 상태로 초기화된다.

## 근거
- `docs/PRD.md` 5.1 메인 메뉴 — 여러 하위 메뉴가 동일한 데이터(시료/주문/생산
  큐)를 공유해야 한다.

## 범위 외
- 실제 메인 루프(입력을 받아 반복 실행)는 다음 증분에서 다룬다.

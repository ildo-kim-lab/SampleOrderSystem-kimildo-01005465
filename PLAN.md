# PLAN.md (이번 증분)

## 목표
시료(Sample)를 시스템에 등록하면, 등록된 시료 목록에서 조회할 수 있다.
Phase 1(MVC 스켈레톤)의 가장 첫 도메인 기능이며, 이후 주문/생산/출고 로직이
이 위에 쌓인다.

## 검증할 동작
`SampleRegistry.register()`로 시료 하나를 등록하면, `SampleRegistry.get_all()`
목록에 그 시료가 포함된다.

## 근거
- `docs/PRD.md` 5.2 시료관리 — 시료 등록(입력값: 시료 ID, 이름, 평균 생산시간, 수율),
  시료 조회
- `docs/PRD.md` 3. 용어집 — 시료(Sample) 정의

## 범위 외
- 시료 검색, 재고 표시, 콘솔 UI는 이번 증분에 포함하지 않는다.
- 중복 ID 등록 시 처리(에러/덮어쓰기 등)는 다음 증분에서 다룬다.

# PLAN.md (이번 증분)

## 목표
`_select_order_by_status`에서 목록에 없는 번호(범위 밖 인덱스)를 입력하면
안내 메시지를 출력하고 `None`을 반환한다 (`IndexError` 대신).

## 검증할 동작
승인 대상 주문이 1개 있는 상태에서 "99"(존재하지 않는 번호)를 입력하면,
안내 메시지가 출력되고 예외 없이 주문 메뉴로 돌아간다.

## 근거
- `CLAUDE.md`의 견고성 원칙을 범위 밖 인덱스 입력까지 확장한다.

## 범위 외
- 숫자가 아닌 입력(`"abc"` 등)에 대한 처리는 다음 증분에서 다룬다.

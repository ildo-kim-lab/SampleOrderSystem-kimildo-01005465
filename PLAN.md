# PLAN.md (이번 증분)

## 목표
이미 등록된 시료 ID로 다시 등록을 시도하면 `SampleRegistry`가 명확한
예외를 발생시킨다 (중복 등록 방지).

## 검증할 동작
`SampleRegistry.register()`를 이미 등록된 `sample_id`를 가진 시료로 다시
호출하면 `ValueError`가 발생한다.

## 근거
- 시료 등록 최초 증분(PLAN.md 히스토리)에서 "범위 외"로 명시했던 항목 —
  중복 ID 등록 시 처리

## 범위 외
- 콘솔 계층에서 이 예외를 사용자 친화적 메시지로 감싸는 것은 다음 증분에서
  다룬다.

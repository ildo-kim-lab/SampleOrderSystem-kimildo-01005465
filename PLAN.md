# PLAN.md (이번 증분)

## 목표
`create_order`에서 주문 수량에 숫자가 아닌 값을 입력하면, 크래시 없이
안내 메시지가 출력된다.

## 검증할 동작
`create_order`에 주문 수량으로 숫자가 아닌 문자열("abc")을 입력하면,
`ValueError`가 그대로 튀어나오지 않고 안내 메시지가 `output_func`로
출력되며, 주문이 등록되지 않는다.

## 근거
- 이전 증분(`register_sample` 숫자 입력 검증)과 동일한 견고성 원칙

## 범위 외
- 이 증분으로 콘솔의 숫자 입력 검증 관련 항목이 마무리된다.

# AGENTS.md

이 문서는 SampleOrderSystem 프로젝트를 여러 에이전트(또는 여러 세션)로 나눠 개발할 때
참고할 **역할 가이드**입니다. `.claude/agents/`에 실행 가능한 커스텀 subagent를 따로
정의하지 않고, 이 문서 하나로 역할과 경계를 문서화하는 절충안입니다.

비즈니스 요구사항은 [docs/PRD.md](docs/PRD.md), 엔지니어링 규칙은 [CLAUDE.md](CLAUDE.md)를
항상 우선 참고하세요.

## 역할 구분

### 1. Domain 담당
- 범위: `Sample`, `Order`, `ProductionLine`, `Stock` 등 도메인 모델, 주문 상태 머신,
  재고 확인, 생산량(`ceil(부족분/수율)`)·생산 시간 계산, 생산 큐(FIFO)
- 원칙: 콘솔 I/O(`input`/`print`)에 의존하지 않는 순수 로직으로 작성. 상태 전이는
  하나의 함수/클래스를 통해서만 발생. `REJECTED`/`RELEASED`는 종료 상태로 이후 전이 금지.

### 2. Console(UI) 담당
- 범위: 메인 메뉴 및 하위 메뉴(시료관리/주문/모니터링/출고 처리/생산 라인) 화면 흐름,
  입력 파싱·검증, 출력 포맷팅
- 원칙: 도메인 로직을 직접 구현하지 않고 Domain 계층 API만 호출. 필요한 도메인 API가
  없으면 직접 만들지 말고 Domain 담당에게 요청 사항으로 명시.

### 3. Persistence 담당
- 범위: 시료/재고/주문/생산 큐를 JSON 파일로 저장·로드 (`docs/PRD.md` 5.8 참고)
- 원칙: 저장/로드 실패 시 사용자에게 명확히 알리고, 파일이 없을 때는 빈 상태로 정상 시작.

### 4. Test 담당
- 범위: pytest 기반 단위/통합 테스트
- 우선순위: 상태 전이(승인 2가지 케이스/거절/생산완료/출고) → 재고·생산량 경계값 →
  생산 큐 FIFO → 영속화 저장/로드 왕복(round-trip) 테스트
- 원칙: 콘솔 I/O는 목(mock)으로 격리하고 도메인 로직은 직접 호출해서 검증.

## 개발 Phase

Phase별 목표와 각 Phase에서 사람이 직접 테스트할 항목은 [docs/PLAN.md](docs/PLAN.md)에
기록합니다. 새 Phase를 시작하거나 목표를 조정할 때는 이 문서가 아니라 `docs/PLAN.md`를
갱신하세요.

## 사용 방법

세션(또는 Claude Code의 `Agent` 도구로 띄우는 subagent)을 시작할 때, 위 역할 중 하나를
프롬프트에 명시해서 경계를 지키게 하세요. 예:

> "Domain 담당으로서 주문 승인 로직을 구현해줘. `docs/PRD.md` 5.4, `CLAUDE.md`의
> 상태 머신 규칙을 따르고, 콘솔 I/O는 건드리지 마."
